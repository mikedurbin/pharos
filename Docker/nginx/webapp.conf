# /etc/nginx/sites-enabled/webapp.conf:
server {
    listen 80;
    server_name _;
    root /home/app/pharos/public;

    passenger_enabled on;
    passenger_user app;

    # If this is a Ruby app, specify a Ruby version:
    passenger_ruby /usr/bin/ruby2.3;
}


